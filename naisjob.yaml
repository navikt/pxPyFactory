apiVersion: nais.io/v1
kind: Naisjob
metadata:
  labels:
    team: dvh-analytisk-plattform
  name: pxpyfactory-test
  namespace: dvh-analytisk-plattform
spec:
  image: "{{ image }}"
  schedule: "*/5 8-18 * * 1-5"
  